# FOCAL RPS — environment configuration.
# Copy to `.env.local` and fill in to switch from the local (IndexedDB) data
# layer to Supabase. Leave BLANK to keep the local-first provider.

# From Supabase → Project Settings → API
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
